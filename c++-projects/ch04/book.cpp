#include <iostream>
#include <string>
#include <vector>
#include <ctime>

struct Book {
    std::string title;
    std::vector<std::string> authors;
    std::time_t publication_date;
};

int main(int argc, char** argv) {
    return 0;
}
